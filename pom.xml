<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ru.textanalysis.tawt</groupId>
    <artifactId>tawt-parent</artifactId>
    <name>_parent</name>
    <description>TAWT parent module</description>
    <packaging>pom</packaging>
    <version>0.0.2</version>

    <modules>
        <module>graphematic-parser</module>
        <module>ms</module>
        <module>jmorfsdk</module>
        <module>gama</module>
        <module>rfc</module>
        <module>awf</module>
        <module>sp</module>
    </modules>

    <properties>
        <project.java.version>1.8</project.java.version>
        <project.build.sourceEncoding>utf-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>${project.build.sourceEncoding}</project.reporting.outputEncoding>

        <version.tawt.graphematic-parser>0.0.2</version.tawt.graphematic-parser>
        <version.tawt.ms>1.2.5</version.tawt.ms>
        <version.tawt.jmorfsdk>2.10.9</version.tawt.jmorfsdk>
        <version.tawt.gama>1.1.3</version.tawt.gama>
        <version.tawt.rfc>0.0.2</version.tawt.rfc>
        <version.tawt.awf>1.1.2</version.tawt.awf>
        <version.tawt.sp>1.1.3</version.tawt.sp>
    </properties>

    <dependencyManagement>
        <dependencies>
<!--            TAWT-->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>parser</artifactId>
                <version>${version.tawt.parser}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>ms</artifactId>
                <version>${version.tawt.ms}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>jmorfsdk</artifactId>
                <version>${version.tawt.jmorfsdk}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>gama</artifactId>
                <version>${version.tawt.gama}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>rfc</artifactId>
                <version>${version.tawt.rfc}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>awf</artifactId>
                <version>${version.tawt.awf}</version>
            </dependency>

<!--            jalexpr-->
            <dependency>
                <groupId>com.github.jalexpr</groupId>
                <artifactId>TemplateWrapperClasses</artifactId>
                <version>12223956f2</version>
            </dependency>

<!--            Logger-->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>1.7.26</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>1.2.3</version>
            </dependency>

<!--            JUnit-->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>5.5.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.1.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.1.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.8.0</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${project.java.version}</source>
                    <target>${project.java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>jitpack</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>
</project>